<Window x:Class="Login.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="180" Width="250">
    <StackPanel Margin="10">
        <Label Target="{Binding ElementName=txtName}"/>
        <StackPanel Orientation="Horizontal">
            <AccessText Text="_UserName:"/>
        </StackPanel>
        <TextBlock Name="txtName">
            <TextBox Width="200" Text="{Binding UserName}"/>
        </TextBlock>
        <Label Target="{Binding ElementName=txtPassword}"/>
        <StackPanel Orientation="Horizontal">
            <AccessText Text="_Password:"/>
        </StackPanel>
        <TextBlock Name="txtPassword">
            <TextBox Width="200" Text="{Binding Password}"/>
        </TextBlock>
        <Button Width="70" Content="Login" Margin="10" Click="Button_Click"/>
    </StackPanel>
</Window>
